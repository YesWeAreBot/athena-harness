// Reserved for the Phase 3 message history store (see docs/06-progress-and-roadmap.md).
// The IM capability itself lives in `@athena-ai/capability-message`; this package will hold the
// durable message log that Memory reads from. Empty on purpose — nothing is wired up yet.

export {};
