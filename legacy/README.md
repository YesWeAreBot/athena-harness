# Legacy

This directory contains archived source from earlier prototype iterations.

- `harness-core/` is the legacy `@yesimbot/harness-core` package. It is no longer part of the
  Yarn workspace or Turbo build/test scope and is preserved for historical reference only.

The canonical core is the `@athena/*` package suite. See `CONTEXT-MAP.md` at the repository root.
